{
    "builds": [
      {
        "src": "./index.js",
        "use": "@vercel/node",
        "config": {
            "buildCommand": "npm run build",
            "outputDir": "/dist"
          }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ]
  }