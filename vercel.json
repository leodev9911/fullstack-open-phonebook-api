{
    "builds": [
      {
        "src": "./",
        "use": "@vercel/build",
        "config": {
            "buildCommand": "npm run build",
            "outputDir": "/dist"
          }
      }
    ],
    "routes": [
      {
        "src": "/",
        "dest": "/index.html"
      }
    ]
  }